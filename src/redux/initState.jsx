import {InitState as ListInitState} from '@/list/index'
import {initState as DetailInitState} from '@/detail/index'

const initState = {
  productList: ListInitState,
  productDetail: DetailInitState
}

export default initState
